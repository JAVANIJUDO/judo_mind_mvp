import '../technique_model.dart';
import 'technique_bundle.dart';

/// Temporary mapper used during migration.
/// Implementation will be completed gradually.
class TechniqueBundleMapper {
  const TechniqueBundleMapper();

  TechniqueBundle fromTechnique(TechniqueModel technique) {
    throw UnimplementedError(
      'TechniqueBundle mapping will be implemented in the next migration phase.',
    );
  }
}